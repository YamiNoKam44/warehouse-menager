{% extends 'base.html.twig' %}

{% block title %}Przyjęcie towaru | Warehouse Manager{% endblock %}

{% block body %}
<header class="content-header">
    <div>
        <p class="eyebrow">Magazyn</p>
        <h1>Przyjęcie towaru</h1>
        <p class="muted">Zarejestruj dostawę artykułu do wybranego magazynu.</p>
    </div>
</header>

<section class="content-panel content-panel--form" aria-labelledby="stock-receipt-form-heading">
    <h2 id="stock-receipt-form-heading" class="visually-hidden">Dane przyjęcia</h2>

    {% if saved %}
        <div class="notice" role="status">
            Przyjęcie towaru zostało zapisane.
        </div>
    {% endif %}

    {{ form_start(form, {attr: {class: 'stock-receipt-form'}}) }}
        {% set root_errors = form_errors(form) %}
        {% if root_errors|striptags|trim is not empty %}
            <div class="alert" role="alert">
                {{ root_errors|raw }}
            </div>
        {% endif %}

        <div class="form-field">
            {{ form_label(form.warehouse) }}
            {{ form_widget(form.warehouse) }}
            {{ form_errors(form.warehouse) }}
        </div>

        <div class="form-field">
            {{ form_label(form.article) }}
            {{ form_widget(form.article) }}
            {{ form_help(form.article) }}
            {{ form_errors(form.article) }}
        </div>

        <div class="form-field">
            {{ form_label(form.quantity) }}
            {{ form_widget(form.quantity) }}
            {{ form_help(form.quantity) }}
            {{ form_errors(form.quantity) }}
        </div>

        <div class="form-field">
            {{ form_label(form.vatRate) }}
            {{ form_widget(form.vatRate) }}
            {{ form_errors(form.vatRate) }}
        </div>

        <div class="form-field">
            {{ form_label(form.unitNetPrice) }}
            {{ form_widget(form.unitNetPrice) }}
            {{ form_help(form.unitNetPrice) }}
            {{ form_errors(form.unitNetPrice) }}
        </div>

        <div class="form-field">
            {{ form_label(form.documents) }}
            {{ form_widget(form.documents) }}
            {{ form_help(form.documents) }}
            {{ form_errors(form.documents) }}
        </div>

        <div class="form-actions">
            <button type="submit">Przyjmij towar</button>
        </div>
    {{ form_end(form) }}
</section>
{% endblock %}
